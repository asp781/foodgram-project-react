# запуск web и postgres

cd /d/dev/foodgram-project-react2
docker-compose up
docker container ls -a 
docker exec -it d9eae765482e bash # Зайти в контейнер
python manage.py makemigrations # создает файлы миграций
python manage.py migrate # создает таблицы в БД
python manage.py loaddata fixtures.json
python manage.py collectstatic --no-input
exit

# не заходя в контейнер:
cd /d/dev/foodgram-project-react2/infra
docker-compose exec backend python manage.py migrate
docker-compose exec backend python manage.py loaddata fixtures.json
docker-compose exec backend python manage.py collectstatic --no-input 

# Запуск front и nginx

cd /d/dev/foodgram-project-react2/infra
docker-compose up



НОВОЕ:

cd /d/dev/foodgram-project-react2/infra
docker-compose up







docker-compose up -d --build 


docker-compose build --no-cache # чтобы собилалось с самого начала

docker ps = docker container ls

docker exec -it <id container> bash # заходим  внутрь контейнера

apt-get update && apt-get install procps